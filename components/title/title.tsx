import React from 'react';
import classes from "./Title.module.css"

const Title = () => {
    return (
        <div className={classes.title}>
            <div className={'font-effect-fire'}>Tattoo Artist</div>
        </div>

    );
};

export default Title;